---
title: "C语言中预处理"
date: "2026-02-15T15:57:22+08:00"
draft: false
categories: ["C"]
tags: ["预处理", "宏", "define", "include"]
---
## 1.预定义符号

1.  \_\_FILE\_\_ //进行编译的源文件
2.  \_\_LINE\_\_ //文件当前的行号
3.  \_\_DATE\_\_ //文件被编译的日期
4.  \_\_TIME\_\_ //文件被编译的时间
5.  \_\_STDC\_\_ //如果编译器遵循ANSI C，其值为1，否则未定义

这些预定义符号是可以直接使用的，这些都是在预处理期间处理的

```c
#include <stdio.h>
```


```c
int main()
{
    printf("源文件：% s\n行号：% d", __FILE__, __LINE__);
    return 0;
}
```


![](/images/c/80d124fb14ae48eea957bd328a005443.png)

## 2.#define定义常量

基本语法

- \#define name stuff

就是给stuff取了一个name的名字，之后写name其实就是stuff

**define定义标识符的时候不要在最后加；否则可能导致语法错误**

## 3.#define定义宏

\#define机制包括了一个规定，允许把参数替换到文本中，这种实现通常称为宏或定义宏

宏的声明方式

- \#define name(parameters-list) stuff

**参数列表的左括号必须与name紧密相连，如果两者之间有任何空白，参数列表就会被解释为stuff的一部分**

例如

```c
#include <stdio.h>
```


```c
#define SQUARE(x) x*x
```


```c
int main()
{
    printf("%d", SQUARE(5));
    return 0;
}
```


这样子就会打印出25，可是如果换一种方式

```c
#include <stdio.h>
```


```c
#define SQUARE(x) x*x
```


```c
int main()
{
    int a = 5;
    printf("%d", SQUARE(a + 1));
    return 0;
}
```


答案会是什么呢

![](/images/c/78ffd8a4dbad4ad68b5b80d92f5af48a.png)

11？似乎与我们预料的36不符，为什么呢？

实际上SQUARE(a + 1)变成了a + 1 \* a + 1，于是答案就变成了11

这个问题也很好解决，就是在定义宏时加上两个括号

```c
#include <stdio.h>
```


```c
#define SQUARE(x) (x)*(x)
```


```c
int main()
{
    int a = 5;
    printf("%d", SQUARE(a + 1));
    return 0;
}
```


**在定义宏时一定不要吝啬自己的括号，用于对数值表达式进行求值的宏定义都应该用这种方式加上括号，避免在使用宏时由于参数中的操作符或邻近操作符之间不可预料的相互作用**

## 4.带有副作用的宏参数

当宏参数在宏的定义中出现超过一次的时候，如果参数带有副作用，那么你在使用这个宏的时候就可能出现危险，导致不可预测的后果。副作用指的是表达式求值的时候出现的永久性效果。

比如x+1就没有副作用但是如果是x++那就会有副作用，因为这会改变x的值

```c
#include <stdio.h>
```


```c
#define MAX(a, b) ( (a) > (b) ? (a) : (b) )
```


```c
int main()
{
    int x = 5;
    int y = 8;
    int z = MAX(x++, y++);
    printf("x=%d y=%d z=%d\n", x, y, z);//输出的结果是什么？ 
    return 0;
}
```


z = ((x++) > (y++) ? (x++) : (y++));

所以输出结果是x=6 y=10 z=9（y实际上被加了两次）

## 5.宏替换的规则

程序中扩展#define定义符号和宏时，涉及以下的步骤

1.  在调用宏时，首先对参数进行检查，看看是否包含任何由#define定义的符号。如果是，他们首先会被替换
2.  替换文本随后被插入到程序中原来文本的位置。对于宏，参数名被他们的值所替换
3.  最后会再次对结果文件进行扫描，看看它是否包含任何由#define定义的符号。如果有重复以上步骤

宏参数和#define定义中可以出现其他#define定义的符号。但是对于宏，不能出现递归。

当预处理器搜索#define定义的符号的时候，字符串常量的内容并不被搜索。

## 6.宏参数的对比

宏通常被应用于执行简单的运算

例如在两个数中找出较大的那一个时，写成宏会更有优势

```c
#define MAX(a, b) ((a) > (b) ? (a) : (b))
```


原因就是用于调用函数和从函数返回的代码可能比实际执行这个小型计算工作所需要的时间更多。所以宏会比函数在程序的规模和速度上更好

更为重要的是函数的参数必须声明为特定的类型，所以函数只能在类型合适的表达式上使用，而宏可以适用于整型，浮点型，长整型等可以用于来比较的类型。宏的参数是类型无关的

和函数相比宏的劣势

- 每次使用宏的时候，一份宏定义的代码将插入到程序中，除非宏比较短，否则可能会大幅度增加程序的长度
- 宏是没办法调试的
- 宏由于类型无关，也就不够严谨
- 宏可能会带来运算符优先级的问题，导致程序容易出错

总结宏和函数的区别

| 属性 | \#define定义宏 | 函数 |
|----|----|----|
| 代码长度 | 每次使用时候，宏代码都会被插入到程序中，除非非常小的宏之外，程序的长度会大幅度增长 | 函数代码只出现在一个地方，每次使用函数时都调用那个地方的同一个代码 |
| 执行速度 | 更快 | 存在函数的调用和返回的额外开销，所以相对慢一些 |
| 操作符优先级 | 宏参数的求值是在所有周围表达式的上下文环境里，除非加上括号，否则操作符的优先级可能会产生不可预料的后果，所以建议宏在书写的时候尽量写括号 | 函数参数只在函数调用的时候求值一次的结果值传递给函数 |
| 带有副作用的参数 | 参数可能被替换到程序中多个位置，如果宏的参数被多次计算，带有副作用的参数求值可能会产生不可预料的后果 | 函数参数只在传参的时候求值一次，很容易控制 |
| 参数类型 | 宏的参数与类型无关，只要对参数的操作是合法的，他就可以使用于任何参数类型 | 函数的参数是与类型有关的，如果参数类型不同，就需要不同的函数 |
| 调试 | 宏是不能调试的 | 函数是可以逐语句调试的 |
| 递归 | 宏是不能递归的 | 函数是可以递归的 |

## 7.#和##

### 7.1#运算符

\#运算符将宏的一个参数转换为字符串字符串字面量。它仅允许出现在带参数的宏的替换列表中

\#运算符所执行的操作可以理解为“字符串化”

例如当定义一个变量int a = 5;的时候，如果想打印the value of a is 10

就可以写成#define PRINT(n) printf("the value of "#n" is %d", n)

```c
#include <stdio.h>
```


```c
#define PRINT(n) printf("the value of "#n" is %d", n)
```


```c
int main()
{
    int a = 5;
    PRINT(a);
    return 0;
}
```


![](/images/c/70d40a6587524e57bb13963e423ec09f.png)

### 7.2##运算符

\##可以把位于它两边的符号合成一个符号，它允许宏定义从分离的文本片段创建标识符。##被称为记号粘合

这样的连接必须产生一个合法的标识符，否则其结果就是未定义的

比如我们想写一个函数求两个数中的较大值是，不同类型就得写不同的函数

```c
int int_max(int x, int y)
{
    return x > y ? x : y;
}
```


```c
float float_max(float x, float y)
{
    return x > y ? x : y;
}
```


而如果使用宏定义就可以写成

```c
#define GENERIC_MAX(type) \
type type##_max(type x, type y) \
{                               \
      return (x>y?x:y);         \
}
```


```c
GENERIC_MAX(int)
GENERIC_MAX(float)
```


```c
int main()
{
    int a = int_max(2, 3);
    printf("%d", a);
    return 0;
}
```


GENERIC_MAX(type)中type可以传任意类型

**这里就会有人觉得宏定义不用##不是也可以吗，这样的话预处理器会认为type_max是一个完整的单词，type不会被替换，这样的话不管传什么类型，都叫type_max**

**// 注意函数名：它没变成
int_max，而是保持原样 type_max  
int type_max(int x, int y)   
{  
    return ...;   
}**

**GENERIC_MAX(int)    // 生成了函数：int
type_max(...)  
GENERIC_MAX(float)  // 生成了函数：float type_max(...)**

**错误原因就是符号重定义，因为不能有两个同名的函数**

事实##用到的也很少，了解即可

## 8.命名约定

一般来讲函数和宏的使用语法很相似，语言本身没法帮我们区别二者，于是我们平时会将宏名字全大写，函数名不要全部大写

## 9.#undef

`#undef` 是 C 语言预处理指令中的\*\*“撤销键”\*\*。

简单来说，#`define` 是用来定义一个宏，而 \#`undef`
是用来取消（Undefined）这个宏的定义。一旦使用了
\#`undef`，编译器就会当做这个宏从来没存在过。

使用语法：#undef 宏的名字

## 10.命令行定义

许多C的编译器提供了一种能力，允许在命令行中定义符号，用于启动编译过程，变长数组就是一种，但是VS2022不支持变长数组

## 11.条件编译

在编译一个程序的时候我们如果要将一条语句（一组语句）编译或者放弃是很方便的，因为我们有条件编译指令

- \#if：如果后面的条件成立（非零），就保留这段代码
- \#elif：（Else If）如果上面的不成立，看看这个条件成不成立
- \#else：如果上面的都不成立，就保留这段代码
- \#endif：结束标志。告诉预处理器，判断逻辑到此为止。（这个必不可少）

<!-- -->

```c
#define WINDOWS_VERSION 10
```


```c
#if WINDOWS_VERSION >= 11
    // 只有当版本 >= 11 时，这行代码才会被编译进去
    printf("正在使用 Windows 11 或更高版本，启用圆角窗口特效。\n");
```


```c
#elif WINDOWS_VERSION == 10
    // 如果上面的不满足，且版本是 10，这行会被编译
    printf("正在使用 Windows 10，启用磁贴特效。\n");
```


```c
#else
    // 如果上面都不满足，这行会被编译
    printf("老版本系统，使用经典主题。\n");
```


```c
#endif // 必须有这个结束符！
```


- \#ifdef：这个是if defined的缩写，如果定义过这个宏，就保留代码
- \#ifndef：这个是if not defined的缩写，如果没有定义过这个宏，就保留代码

<!-- -->

```c
#include <stdio.h>
```


```c
// 打开这个开关（定义它），就会打印日志
// 注释掉这行，日志代码就仿佛“消失”了
#define DEBUG_MODE 
```


```c
int main() {
    printf("程序正在运行...\n");
```


```c
#ifdef DEBUG_MODE
    // 只有定义了 DEBUG_MODE，这行才会存在
    printf("[调试信息] 变量 x 的值是 100\n");
#endif
```


```c
    return 0;
}
```


## 12.头文件的包含

### 12.1本地文件包含

**写法：** `#include "my_file.h"` (使用双引号)

**查找策略（关键区别）：**

1.  **先找当前目录：** 编译器首先会在源文件（.c）所在的文件夹里找。

2.  **再找系统目录：** 如果当前文件夹没找到，才会去系统默认的库目录找。

### 12.2库文件包含

**写法：** `#include <stdio.h>` (使用尖括号)

**查找策略：**

1.  **直接去系统目录：**
```c
编译器**忽略**当前文件夹，直接去环境变量配置好的系统路径（Include
Path）里找。
```


**虽然你可以用 `""` 来引用标准库（比如
`#include "stdio.h"`
也能跑通，因为在当前目录找不到后它还是会去系统找），但这是一种极不推荐的坏习惯，会让代码可读性变差，且稍微拖慢编译速度。**

### 12.3嵌套文件

就是在一个头文件里又包含另外一个头文件，但是容易出现重复保护

我们可以利用上述学到的条件编译来避免重复包含或者写#pragma
once来避免头文件的重复引入

## 13.其他预处理指令

- \#error
- \#pragma
- \#line
- \#pragma pack()
